{
  "name": "ts-ddd-common",
  "version": "0.1.0",
  "description": "common class for DDD",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": ["DDD"],
  "module": "build/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ddd-tw/ts-ddd-common.git"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "devDependencies": {
    "@types/node": "^10.0.3",
    "gts": "^1.1.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "~3.5.0"
  },
  "dependencies": {
    "@types/jest": "^24.0.22"
  }
}
